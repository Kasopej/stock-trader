/* eslint-disable no-unused-vars */
const state = {};
const getters = {};
const actions = {
  saveNewUserAccount({ state }, payload) {
    //payload = { ...payload, wallet: 0, portfolio: [] };
    //axios.post("users.json" + `?auth=${state.idToken}`, payload);
  },
  updateUserAccount() {},
  fetchUserAccount() {},
};
const mutations = {};
